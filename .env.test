# Test environment — used by pytest via: pytest --env-file=.env.test
# SQLite is used instead of PostgreSQL so tests run without a DB server.
DATABASE_URL=sqlite:///./test.db
SECRET_KEY=test-secret-key-not-for-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
